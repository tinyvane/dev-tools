name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Cancel in-flight runs of the same workflow on the same ref.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ['3.11', '3.12', '3.13']

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip

      - name: Install package + dev deps
        run: >-
          python -m pip install --upgrade pip &&
          python -m pip install -e ".[dev]" -e "./tools/portablecodex"

      - name: Show package versions
        run: codesync --version && portablecodex --version

      - name: Run Ruff
        run: python -m ruff check .

      - name: Run codesync pytest
        run: python -m pytest tests/ -v

      - name: Run PortableCodex pytest
        run: python -m pytest tools/portablecodex/tests/ -v
